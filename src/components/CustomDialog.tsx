export type CustomDialogState = {
    isOpen: boolean;
    content: JSX.Element;

  }

// handling queued popups, handling callbacks/ notifying the calling element?
//
//

export function QueueNewDialog() {};

export default function CustomDialog() {
  return (<>
    
  </>);
}
