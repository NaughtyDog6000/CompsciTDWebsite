import { Button } from "@/components/ui/button";

// have the link default to the current page.
// have an option to be able to set the link.
// provide a copyable link / a copy to clipboard button.

export default function SharePageButton() {
  return (
    <>
      <Button>Share</Button>
    </>
  );
}
